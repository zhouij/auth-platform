
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/resourcedb
    username: resource_user
    password: resource_pass
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9081
          jwk-set-uri: http://auth-server:9081/oauth2/jwks
